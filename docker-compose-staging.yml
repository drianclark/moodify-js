version: "3.7"

services:

    backend:
        build: ./server
        command: npm run start
        environment: 
          - NODE_ENV=staging
          - URL=http://35.230.144.118
          - PORT=80
        ports:
          - '80:80'

    db_update_service:
        build: ./db_update_service
        command: npm run start
        environment: 
          - NODE_ENV=staging
          - URL=http://35.230.144.118
          - PORT=80
        depends_on:
          - backend
